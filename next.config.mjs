/** @type {import('next').NextConfig} */
const nextConfig = {
    transpilePackages: [ 'genpixels_ui_components'],
    images: {
        domains: ['strong-bee-4932a093ba.media.strapiapp.com'],
      },
};

export default nextConfig;
